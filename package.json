{
  "name": "cheto-video-ai",
  "version": "0.2.0",
  "private": true,
  "description": "Base de escritorio para CHETO VIDEO AI",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @cheto/desktop",
    "build": "npm run build --workspace @cheto/desktop",
    "typecheck": "npm run typecheck --workspace @cheto/desktop",
    "lint": "npm run lint --workspace @cheto/desktop",
    "test": "npm run test --workspace @cheto/desktop",
    "tauri": "npm run tauri --workspace @cheto/desktop"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
