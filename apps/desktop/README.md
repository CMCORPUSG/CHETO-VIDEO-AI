# Desktop

Shell de escritorio de CHETO VIDEO AI basado en Tauri, React y TypeScript.

```powershell
npm run dev
```

El frontend es ejecutable sin Rust. La ventana nativa requiere el toolchain descrito en `../../docs/DEVELOPMENT.md`.
