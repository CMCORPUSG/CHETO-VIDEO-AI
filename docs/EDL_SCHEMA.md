# EDL

El EDL conserva `projectId`, `sourceId`, `sourceDurationUs`, `timebase.unit: "microseconds"` y los tracks `cuts`, `camera`, `broll` y `audio`. Las decisiones aceptadas se validan contra la fuente y se escriben atómicamente.
